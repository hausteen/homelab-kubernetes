apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-corefile
  namespace: coredns
data: # the capital C is intentional
  Corefile: |
    lab.hunt.internal {
      file /etc/coredns/zone/lab.hunt.internal {
        reload 60s
        reload_by_mtime
      }
      errors
      loadbalance
      log
      loop
      ready
      reload
      health {
        lameduck 5s
      }
    }
