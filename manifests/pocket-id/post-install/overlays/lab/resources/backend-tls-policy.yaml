apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: pocket-id
  namespace: pocket-id
spec:
  targetRefs:
  - kind: Service
    name: pocket-id-server
    group: ""
  validation:
    hostname: pocket-id-server.pocket-id.svc.cluster.local
    caCertificateRefs:
    - kind: ConfigMap
      name: hunt-internal-trust-bundle
      group: ""
