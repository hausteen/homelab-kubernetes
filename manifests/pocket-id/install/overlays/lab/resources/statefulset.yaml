apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: pocket-id
  namespace: pocket-id
spec:
  selector:
    matchLabels:
      app: pocket-id
  updateStrategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
  replicas: 1
  serviceName: "pocket-id-server-headless" # points to the headless service
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: pocket-id
    spec:
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 10
      containers:
      - name: pocket-id
        image: ghcr.io/pocket-id/pocket-id:v2.13.0-distroless
        ports:
        - containerPort: 1411
          protocol: TCP
        envFrom:
        - configMapRef:
            name: pocket-id
        livenessProbe:
          exec:
            command:
            - /app/pocket-id
            - healthcheck
          initialDelaySeconds: 10
          periodSeconds: 90
          timeoutSeconds: 5
          failureThreshold: 2
        readinessProbe:
          exec:
            command:
            - /app/pocket-id
            - healthcheck
          initialDelaySeconds: 10
          periodSeconds: 90
          timeoutSeconds: 5
          failureThreshold: 2
        volumeMounts:
        - name: secrets
          mountPath: /app/secrets/
          readOnly: false
        - name: data-volume
          mountPath: /app/data/
          readOnly: false
        securityContext:
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 2000
        seccompProfile:
          type: RuntimeDefault
      volumes:
      - name: secrets
        projected:
          sources:
          - secret:
              name: pocket-id-encryption-key
          - secret:
              name: pocket-id-server-certificate
              items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
          - configMap:
              name: hunt-internal-trust-bundle
              items:
              - key: ca.crt
                path: ca.crt
  volumeClaimTemplates:
  - metadata:
      name: data-volume
      namespace: pocket-id
    spec:
      storageClassName: no-replication
      accessModes:
        - ReadWriteOncePod
      resources:
        requests:
          storage: 5Gi
