apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmChart
metadata:
  name: istio-base
  namespace: istio-system
spec:
  chart: base
  version: "1.29.x"
  sourceRef:
    kind: HelmRepository
    name: istio
  interval: 24h
  reconcileStrategy: ChartVersion
