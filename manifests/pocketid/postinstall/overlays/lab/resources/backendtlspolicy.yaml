apiVersion: gateway.networking.k8s.io/v1
kind: BackendTLSPolicy
metadata:
  name: pocketid-server-routing-tlspolicy
  namespace: pocket-id
spec:
  targetRefs:
  - kind: Service
    name: pocketid-server
    group: ""
  validation:
    hostname: pocketid-server.pocket-id.svc.cluster.local
    caCertificateRefs:
    - kind: ConfigMap
      name: huntinternal-trustbundle
      group: ""
