apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pocketid-server-tls
  namespace: pocket-id
spec:
  secretName: pocketid-server-tls
  issuerRef:
    name: huntinternal
    kind: ClusterIssuer
    group: cert-manager.io
  privateKey:
    rotationPolicy: Always
    algorithm: ECDSA
    size: 256
  isCA: false
  subject:
    countries:
    - usa
    organizations:
    - hunt
  dnsNames:
  - pocketid-server
  - pocketid-server.pocket-id
  - pocketid-server.pocket-id.svc
  - pocketid-server.pocket-id.svc.cluster.local
  commonName: app # the db verifies this field
  duration: 720h # 30 days * 24 hours
  renewBefore: 120h # 5 days before it ends. 5 days * 24 hours
